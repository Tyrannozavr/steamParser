# Steam Market Parser

Система для парсинга Steam Market с поддержкой фильтров, прокси ротации, Telegram бота и API.

## 📚 Документация

### API Документация

- **Интерактивная документация (Swagger UI)**: [https://api.yedu.ae/api/docs](https://api.yedu.ae/api/docs)
- **Скачать OpenAPI спецификацию (JSON)**: [https://api.yedu.ae/api/docs/openapi.json](https://api.yedu.ae/api/docs/openapi.json)
- **Скачать OpenAPI спецификацию (YAML)**: [https://api.yedu.ae/api/docs/openapi.yaml](https://api.yedu.ae/api/docs/openapi.yaml)

## 🚀 Быстрый старт

### Требования

- **Docker** 20.10+
- **Docker Compose** 2.0+
- **Git**

### Установка и развертывание

#### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd steam
```

#### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите необходимые параметры:

```env
# База данных PostgreSQL
POSTGRES_USER=steam_user
POSTGRES_PASSWORD=steam_password
POSTGRES_DB=steam_monitor
DATABASE_URL=postgresql+asyncpg://steam_user:steam_password@postgres:5432/steam_monitor

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Telegram Bot
# ВАЖНО: Укажите реальный токен бота, полученный от @BotFather в Telegram
# Если оставить "your_bot_token_here", бот не запустится
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Уровень логирования
LOG_LEVEL=INFO
```

**⚠️ Важно:** 
- Обязательно укажите реальный `TELEGRAM_BOT_TOKEN` от @BotFather
- Без валидного токена Telegram бот не запустится

#### 3. Запуск через Docker Compose

**Production режим:**

```bash
docker compose up -d
```

**Development режим (с hot-reload):**

```bash
docker compose -f docker-compose.dev.yml up -d
```

#### 4. Проверка статуса

Проверьте, что все сервисы запущены:

```bash
docker compose ps
```

Проверьте логи:

```bash
# Все сервисы
docker compose logs -f

# Конкретный сервис
docker compose logs -f telegram-bot
docker compose logs -f parsing-worker
docker compose logs -f parser-api
```

#### 5. Применение миграций БД

Миграции применяются автоматически при запуске Telegram бота. Если нужно применить вручную:

```bash
docker compose exec telegram-bot bash /app/docker/apply-migrations.sh
```

## 📦 Структура проекта

```
steam/
├── core/                    # Основная логика парсинга
│   ├── steam_parser.py     # Главный парсер
│   ├── steam_api_methods.py # API методы Steam
│   └── ...
├── services/                # Сервисы
│   ├── proxy_manager.py    # Управление прокси
│   ├── parsing_service.py  # Сервис парсинга
│   └── ...
├── telegram/                # Telegram бот
│   ├── telegram_bot_app.py # Главное приложение
│   └── handlers/           # Обработчики команд
├── parsing/                 # Worker для парсинга
│   └── parsing_worker.py
├── parser_api/              # REST API
│   └── app.py
├── docker/                  # Docker скрипты
│   └── apply-migrations.sh # Скрипт миграций
├── migrations/              # SQL миграции
├── docker-compose.yml       # Production конфигурация
└── docker-compose.dev.yml   # Development конфигурация
```

## 🔧 Конфигурация

### Переменные окружения

Основные переменные окружения (см. `.env.example`):

- **DATABASE_URL** - URL подключения к PostgreSQL
- **REDIS_HOST**, **REDIS_PORT** - Параметры Redis
- **TELEGRAM_BOT_TOKEN** - Токен Telegram бота
- **TELEGRAM_CHAT_ID** - ID чата для уведомлений
- **LOG_LEVEL** - Уровень логирования (DEBUG, INFO, WARNING, ERROR)

### Настройка прокси

Прокси добавляются через Telegram бота командой `/add_proxy` или через API.

## 🐳 Docker сервисы

### telegram-bot
Telegram бот для управления задачами парсинга.

**Порты:** Не требуется (использует webhook или polling)

**Volumes:**
- `./data:/app/data` - Данные приложения
- `./docker/apply-migrations.sh:/app/docker/apply-migrations.sh:ro` - Скрипт миграций
- `./migrations:/app/migrations:ro` - SQL миграции

### parsing-worker
Воркер для выполнения задач парсинга.

**Volumes:**
- `./data:/app/data`
- `./logs:/app/logs` (только в dev режиме)

### parser-api
REST API для работы с парсером.

**Порты:** `8000:8000`

**Endpoints:**
- `GET /health` - Проверка здоровья
- `GET /version` - Версия API
- `GET /docs` - Swagger документация

### postgres
База данных PostgreSQL.

**Порты:** `5432:5432`

**Volumes:**
- `postgres_data:/var/lib/postgresql/data`

### redis
Кэш и очереди Redis.

**Порты:** `6379:6379`

**Volumes:**
- `redis_data:/data`

## 📝 Использование

### Telegram бот

После запуска найдите бота в Telegram и используйте команды:

- `/start` - Начать работу
- `/add_proxy <url>` - Добавить прокси
- `/list_proxies` - Список прокси
- `/check_proxies` - Проверить прокси
- `/add_task` - Добавить задачу парсинга
- `/list_tasks` - Список задач
- `/help` - Справка

### API

API доступно по адресу `http://localhost:8000` (или ваш домен).

Документация: `http://localhost:8000/docs`

## 🔄 Обновление

1. Остановите контейнеры:
```bash
docker compose down
```

2. Обновите код:
```bash
git pull
```

3. Пересоберите образы (если нужно):
```bash
docker compose build
```

4. Запустите снова:
```bash
docker compose up -d
```

## 🐛 Отладка

### Просмотр логов

```bash
# Все логи
docker compose logs -f

# Конкретный сервис
docker compose logs -f telegram-bot
docker compose logs -f parsing-worker
docker compose logs -f parser-api
docker compose logs -f postgres
docker compose logs -f redis
```

### Подключение к контейнеру

```bash
# Telegram бот
docker compose exec telegram-bot bash

# Parsing worker
docker compose exec parsing-worker bash

# PostgreSQL
docker compose exec postgres psql -U steam_user -d steam_monitor
```

### Проверка подключений

```bash
# Проверка PostgreSQL
docker compose exec postgres pg_isready -U steam_user

# Проверка Redis
docker compose exec redis redis-cli ping
```

## 📊 Мониторинг

### Проверка статуса задач

Через Telegram бота: `/list_tasks`

### Проверка прокси

Через Telegram бота: `/check_proxies`

### Метрики

- Логи доступны через `docker compose logs`
- Данные в БД: таблицы `tasks`, `proxies`, `items`

## 🔒 Безопасность

1. **Никогда не коммитьте `.env` файл** - он содержит секретные данные
2. **Используйте сильные пароли** для PostgreSQL
3. **Ограничьте доступ** к портам базы данных и Redis
4. **Регулярно обновляйте** зависимости

## 📖 Дополнительная документация

Дополнительная документация находится в директории `docs/`.

## 🆘 Решение проблем

### Проблема: Контейнер не запускается

1. Проверьте логи: `docker compose logs <service_name>`
2. Проверьте переменные окружения в `.env`
3. Убедитесь, что порты не заняты другими приложениями

### Проблема: Ошибка подключения к БД

1. Проверьте, что PostgreSQL запущен: `docker compose ps postgres`
2. Проверьте переменные в `.env`
3. Проверьте логи PostgreSQL: `docker compose logs postgres`

### Проблема: Миграции не применяются

1. Убедитесь, что файл `docker/apply-migrations.sh` существует
2. Проверьте права на выполнение: `chmod +x docker/apply-migrations.sh`
3. Примените миграции вручную (см. раздел "Применение миграций БД")

### Проблема: Прокси не работают

1. Проверьте формат прокси: `http://user:pass@host:port`
2. Проверьте доступность прокси: `/check_proxies` в Telegram боте
3. Проверьте логи: `docker compose logs parsing-worker`

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи сервисов
2. Изучите документацию в `docs/`
3. Создайте issue в репозитории

## 📄 Лицензия

[Укажите лицензию]
