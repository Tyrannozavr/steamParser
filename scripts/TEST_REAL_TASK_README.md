# Инструкция по тестированию реальной задачи с паттерном

## Описание

Скрипт `test_real_task_with_pattern.py` находит задачу с паттерном из БД, запускает парсинг и проверяет:
- Что парсинг выполняется успешно
- Что счетчики `total_checks` и `items_found` обновляются
- Что `last_check` и `next_check` устанавливаются
- Что уведомления отправляются (если предметы найдены)
- Что предметы сохраняются в БД

## Запуск через Docker

### Вариант 1: Если контейнеры запущены через docker-compose

```bash
# Запустить тест внутри контейнера parsing-worker
docker exec -it steamparser-parsing-worker python3 /app/scripts/test_real_task_with_pattern.py
```

### Вариант 2: Использовать скрипт-обертку

```bash
# Сделать скрипт исполняемым (если еще не сделано)
chmod +x scripts/run_test_task_docker.sh

# Запустить
./scripts/run_test_task_docker.sh
```

## Запуск локально (если есть доступ к БД)

```bash
# Убедитесь, что переменные окружения установлены
export DATABASE_URL="postgresql+asyncpg://steam_user:steam_password@localhost:5432/steam_monitor"
export REDIS_URL="redis://localhost:6379/0"

# Запустить скрипт
python3 scripts/test_real_task_with_pattern.py
```

## Что проверяет скрипт

1. **Поиск задачи**: Находит случайную активную задачу с паттерном из БД
2. **Запуск парсинга**: Выполняет полный цикл парсинга через `ParsingService`
3. **Проверка результатов**:
   - Успешность парсинга
   - Количество найденных предметов
   - Обновление счетчиков в БД
   - Сохранение предметов в БД
   - Установка `next_check`

## Ожидаемый результат

После успешного выполнения вы должны увидеть:

```
✅ ПАРСИНГ ЗАВЕРШЕН за XX.X секунд
📊 Результат:
   Успех: True
   Всего найдено: X
   После фильтрации: Y
   Предметов в результате: Z

📊 ФИНАЛЬНОЕ СОСТОЯНИЕ ЗАДАЧИ:
   Проверок: X → Y (изменение: +1)
   Найдено: X → Y (изменение: +Z)
   ✅ Счетчик проверок обновлен
   ✅ last_check обновлен
   ✅ next_check установлен
```

## Проверка уведомлений

После выполнения теста проверьте:
1. Логи telegram-bot на наличие уведомлений
2. Telegram чат на наличие сообщений о найденных предметах
3. БД на наличие новых записей в `found_items`

## Устранение проблем

### Ошибка: "Контейнер не запущен"
```bash
# Запустите контейнеры
docker compose up -d
```

### Ошибка: "Нет активных задач"
- Создайте задачу через Telegram бота или скрипт `add_test_task.py`

### Ошибка: "Таймаут при сохранении"
- Проверьте логи PostgreSQL на наличие блокировок
- Убедитесь, что пул соединений БД настроен правильно (см. `core/database.py`)

## Примечания

- Скрипт выбирает случайную задачу с паттерном
- Если задач с паттернами нет, используется первая активная задача
- Тест может занять несколько минут в зависимости от количества страниц
- Уведомления отправляются ДО сохранения в БД (новое поведение после исправлений)
